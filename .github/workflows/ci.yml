name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # SonarCloud needs full git history to compute the "new code" diff
          # for per-PR Quality Gate evaluation (coverage on new code, etc.).
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-groups --python ${{ matrix.python-version }}

      - name: Ruff lint
        run: uv run ruff check

      - name: Ruff format check
        run: uv run ruff format --check

      - name: Bandit security scan
        run: uv run bandit -r src/ -c pyproject.toml

      - name: License scan
        run: uv run pip-licenses --fail-on="GPL;AGPL;LGPL" --format=markdown

      - name: Type check (mypy)
        run: uv run mypy

      - name: Pylint
        run: uv run pylint src/

      - name: Tests with coverage
        run: uv run pytest

      - name: Upload coverage report
        if: matrix.python-version == '3.11'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml
          if-no-files-found: warn

      - name: SonarQube Scan
        # Only scan once per workflow run (avoid duplicate 3.11/3.12 analyses
        # racing against the same SonarCloud project).
        if: matrix.python-version == '3.11'
        uses: SonarSource/sonarqube-scan-action@fd88b7d7ccbaefd23d8f36f73b59db7a3d246602 # v6.0.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
